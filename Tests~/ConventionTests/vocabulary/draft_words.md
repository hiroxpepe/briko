# Draft Words

Words this repository uses that are not in the shared word lists yet. They wait
here for review; the ones that pass are lifted into the opinio word lists, and
then taken out of here. A word here must be a real, spelled-out word, not an
abbreviation — an abbreviation is fixed in the code, not kept here.

+ 2f
+ active
+ already
+ asset assets
+ assign assigns assigned assigning
+ calculate calculates calculated calculating
+ candidate candidates
+ collect collects collected collecting
+ container containers
+ descending
+ differ differs differed differing
+ difference differences
+ dimension dimensions
+ disable disables disabled disabling
+ entity entities
+ exporter exporters
+ ground grounds
+ guid guids
+ hierarchy hierarchies
+ importer importers
+ normalize normalizes normalized normalizing
+ output outputs
+ panel panels
+ parser parsers
+ platform platforms
+ project projects
+ relative
+ rotation rotations
+ scan scans scanned scanning
+ scene scenes
+ snap snaps snapped snapping
+ snapper snappers
+ sorter sorters
+ spawn spawns spawned spawning
+ structural
+ surface surfaces
+ track tracks tracked tracking
+ valid
+ variant variants
+ zone zones
